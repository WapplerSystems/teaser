plugin.tx_teaser_list {
    view {
        # cat=plugin.tx_teaser_list/file; type=string; label=Path to template root (FE)
        templateRootPath = EXT:teaser/Resources/Private/Templates/
        # cat=plugin.tx_teaser_list/file; type=string; label=Path to template partials (FE)
        partialRootPath = EXT:teaser/Resources/Private/Partials/
        # cat=plugin.tx_teaser_list/file; type=string; label=Path to template layouts (FE)
        layoutRootPath = EXT:teaser/Resources/Private/Layouts/
    }
    persistence {
        # cat=plugin.tx_teaser_list//a; type=string; label=Default storage PID
        storagePid =
    }
}
